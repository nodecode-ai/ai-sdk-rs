[package]
name = "generate-stream"
version = "0.1.1"
edition = "2021"
publish = false

[dependencies]
anyhow = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
futures-util = "0.3"
serde_json = "1"
ai-sdk-rs = { path = "../.." }
