[package]
name = "anthropic-thinking"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
futures-util = "0.3"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
serde_json = "1"
ai-sdk-rs = { path = "../.." }
